<%- include('partials/_header') %>
  <!-- Archivos -->    
  <link rel="stylesheet" href="../public/css/general.css">
  <title>Editar usuario</title>
</head>
<body>
  
  <!--NAVBAR-->
  
  <%- layout('layouts/main') %>
  <% if(user){ %>
          
    <% if(user.username == "gkn" ){ %>
      <div class="container-fliud"> <!-- class="container-fliud"-->            
        <div class="row">
            <div class="col-3"></div>
            <div id="formulario" class="col-6 vh-0 d-flex justify-content-center">                  
                <form form action="/changueuser" method="POST">
                    <div class="form-group mx-auto ">
                        <h1>Editar usuario</h1>
                      <label for="exampleInputEmail1">Nombre</label>
                      <input type="text" name="fullname" placeholder="Full Name" class="form-control mb-3" value="<%= rows[0].fullname %>">
                    </div>
                    <div class="form-group">
                      <label for="exampleInputEmail1">Username</label>
                      <input type="text" name="username" placeholder="Username" class="form-control mb-3" value="<%= rows[0].username %>">
                    </div>
                    <div class="form-group">
                      <label for="exampleInputPassword1">Contraseña</label>
                      <input type="password" name="password" placeholder="Password" class="form-control mb-3" value="<%= rows[0].password %>">
                    </div>
                    <button type="submit" class="btn btn-dark mb-3">Guardar </button>
                    <br>
                  </form>
            </div>  
        </div>
    </div>
    <%}else{%>
        <h1>Acceso denegado</h1>
    <% } %>
<% }else{%>
    <h1>Acceso denegado</h1>
<% } %>2
  
  
  
  
<%- include('partials/_footer') %>